FROM node:22-alpine

WORKDIR /app

RUN addgroup --system --gid 1001 nodejs \
  && adduser --system --uid 1001 --ingroup nodejs scheduler

COPY --chown=scheduler:nodejs scripts/queue-scheduler.mjs ./queue-scheduler.mjs

USER scheduler

ENV NODE_ENV=production

ENTRYPOINT ["node", "queue-scheduler.mjs"]
