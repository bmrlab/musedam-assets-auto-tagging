/** Fixed table column width for 关联标签 across library tables. */
export const LINKED_TAGS_COLUMN_WIDTH_PX = 400;

export const linkedTagsColumnHeaderClassName = "w-[400px] max-w-[400px] px-4 py-0";

export const linkedTagsColumnCellClassName = "min-w-0 w-[400px] max-w-[400px]";

/** Horizontal padding on tags cells (`px-4` × 2). */
export const LINKED_TAGS_CELL_PADDING_PX = 32;

export const LINKED_TAGS_CONTENT_MAX_WIDTH_PX =
  LINKED_TAGS_COLUMN_WIDTH_PX - LINKED_TAGS_CELL_PADDING_PX;
